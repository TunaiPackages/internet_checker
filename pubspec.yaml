name: internet_checker
description: "A new Flutter package project."
version: 0.0.1
homepage: "https://github.com/TunaiPackages/internet_checker.git"

environment:
  sdk: ">=3.3.0 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  connectivity_plus: ^6.1.0
